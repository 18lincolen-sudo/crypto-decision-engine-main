# תוכנית ביצוע — תיקוני מנועים

**נוצר:** 2026-09-03
**סטטוס:** ✅ כל השלבים (0-4) בוצעו ונמדדו
**מסמכים קשורים:** [ALG_legacy.md](ALG_legacy.md), [ALG_pro.md](ALG_pro.md), [ALG_intraday.md](ALG_intraday.md), [ARCHITECTURE.md](ARCHITECTURE.md), [REPORT_SIM_BACKTEST_ISSUES.md](REPORT_SIM_BACKTEST_ISSUES.md)

---

## 0. רקע ותיחום

התוכנית נגזרת מניתוח שבחן את ההצעה:

> `builds fair value, flags mispricing > 8% → calculates position size (kelly criterion, max 6% bankroll)`

**המסקנה: ההצעה כלשונה לא מיושמת.** הסף של 8% אינו תואם את גאומטריית הסיכון של המנועים (SL 1.8% / TP1 3.0%), וחצי הקלי כבר קיים. מה שכן חולץ ממנה הוא הצורך ב**גורם אורתוגונלי אחד** — ראו שלב 3.

### מה שלא ייעשה במסגרת תוכנית זו

| נדחה | סיבה |
|---|---|
| מודל "שווי הוגן" סטטיסטי | חופף ל-Bollinger (12) + EMA20/50 (18) + RSI (12) הקיימים; אין מנגנון אכיפה |
| סף mispricing של 8% | פי 4.4 מהסטופ, פי 2.7 מ-TP1. אות שלא משפיע על ההחלטה |
| מסחר נגד דיסלוקציות גדולות | סטייה שלילית חדה; 8% בקריפטו הוא בד"כ מידע, לא טעות תמחור |

### הנחות בסיס

- אין לגעת בלוגיקת הבוטים ללא בקטסט A/B מקדים (ראו שלב 4)
- כל שלב = קומיט נפרד + תג rollback, לפי המוסכמה של `checkpoint-pro-advanced-analysis`
- כלים חינמיים בלבד
- שערי ההחלטה חייבים להמשיך להחזיר `gate` + `blockReason` — יש טסטי רגרסיה שאוכפים זאת ([decisionBlockMetadata.test.ts](src/__tests__/decisionBlockMetadata.test.ts))

---

## שלב 0 — ניקוי ופריסה

**סיכון:** אפס | **מאמץ:** ~30 דקות | **תלות:** אין

זהו השלב היחיד שאפשר לבצע מיד בלי בקטסט.

### P0.1 — הסרת ייבוא מת

`packages/engine/src/services/proSimExecution.ts:24` מייבא `computeProAdvancedAnalysis` ואינו משתמש בו כלל (שארית מקומיט `c8d83ba`).

### P0.2 — יישור תיעוד סותר

שלושה מקומות מתארים את המצב **לפני** ההחזרה של מקור האותות ב-Pro:

| קובץ | שורה | מה כתוב | מה נכון |
|---|---|---|---|
| `ALG_pro.md` | 497 | Layer 1 = `computeProAdvancedAnalysis` | `evaluateProSignals` |
| `ALG_pro.md` | 512 | "`evaluateProSignals` — קוד מת, לא מקור אותות פעיל" | ההפך הגמור מהמצב |
| `REPORT_SIM_BACKTEST_ISSUES.md` | 135-136, 282 | F6 "תוקן" ל-`computeProAdvancedAnalysis` | הוחזר ל-`evaluateProSignals` |

בנוסף — `REPORT_SIM_BACKTEST_ISSUES.md` מתאר גם את פער ה-SL/TP התוך-נרי כפתוח, אך `intrabarExit()` כבר קיים ומלא (`backtestRunner.ts:291-315`). יש לסמן גם אותו כסגור.

### P0.3 — פריסת `c8d83ba` ל-Render

**זהו החוסם היחיד שמונע מהחזרת מקור האותות של Pro להיות חיה.** הקוד נדחף ל-`origin/main`; השרת עדיין מריץ את הבילד הקודם.

### אימות שלב 0

```
npm run typecheck && npm run typecheck:worker
npm run build && npm run build:worker
npm test                                    # ציפייה: 238 עוברים
```
ואז בדיקת `/api/pro/state` בשרת החי מול תוצאה מקומית על אותו סימבול.

**Rollback:** `git revert` — אין שינוי התנהגותי.

### ✅ בוצע — 2026-09-03

| משימה | סטטוס |
|---|---|
| P0.1 — הסרת הייבוא המת | ✅ `proSimExecution.ts` |
| P0.2 — יישור התיעוד | ✅ `ALG_pro.md` (§89, §281, §386, §497, §512) + `REPORT_SIM_BACKTEST_ISSUES.md` (6א, 6ב, F6, F10) |
| P0.3 — פריסה ל-Render | ⏳ **פעולה של המשתמש** — Manual Deploy נדרש |

**אימות:** typecheck ×2 נקי · build ×2 עובר · 238 טסטים עוברים (2 מדולגים)

**תיקון שהתגלה תוך כדי:** גם `ALG_pro.md:89` (LAYER 1) ו-`ALG_pro.md:281` (§4 Confidence) הצביעו על `computeProAdvancedAnalysis`, מעבר לשלושת המקומות שזוהו בתכנון. סומנו כלא-עדכניים עם הפניה ל-§10; הטקסט המקורי נשמר כלשונו לפי ההנחיה לא לערוך את סעיפים 1-9.

**תיקון נוסף:** `REPORT_SIM_BACKTEST_ISSUES.md` F10 טען שה-providers תוחמו ל-`SimulationBot.tsx`. הם **הוחזרו מאז ל-`App.tsx:52-67`** (התיחום ניתק את דף הבית ממצב השרת). סומן.

---

## שלב 1 — תיקון מדידת הקלי

**סיכון:** בינוני (משנה גודל פוזיציה בכל העסקאות) | **מאמץ:** ~4-6 שעות | **תלות:** שלב 0

### הבעיה

הקלי מחושב על **PnL דולרי**, לא על יחידות סיכון:

```ts
// tradeEngine.ts:1328-1333  |  proAlgEngine.ts:617-621 — זהה
const avgWin  = winning.reduce((s, t) => s + t.pnl, 0) / winning.length;
const avgLoss = Math.abs(losing.reduce((s, t) => s + t.pnl, 0) / losing.length);
const historicalR = avgLoss > 0 ? avgWin / avgLoss : riskRewardRatio;
kellyFraction = winRate - (1 - winRate) / historicalR;
```

מכיוון ש-`betFraction` עצמו דינמי, גדלי הפוזיציות משתנים בין עסקאות — ולכן `historicalR` מזוהם על ידי הסייזינג ואינו יחס התמורה. **הקלי מזין את עצמו בפלט של עצמו:** רצף רווחים בפוזיציות גדולות מנפח את R, שמנפח את `betFraction`, שמנפח את R הבא. לולאת משוב חיובית בפרמטר שקובע חשיפה.

בעיה שנייה: הסף `closedTrades.length >= 30`. ב-n=30 שגיאת התקן של שיעור ההצלחה היא ±9.1 נק' אחוז. ב-R=1.67:

| הערכת win rate | Kelly | חצי-קלי בפועל |
|---|---|---|
| 50% (המדידה) | 0.201 | **10.0%** (בתקרה) |
| 41% (σ אחת למטה) | 0.057 | **2.8%** |

גודל הפוזיציה קופץ פי 3.5 מרעש דגימה טהור.

### הביצוע

**1a — הרחבת רשומת העסקה הסגורה**

`packages/engine/src/services/adaptiveRisk.ts:26` — הוספת שדה **אופציונלי**:

```ts
export interface ClosedTradeRecord {
  pnl: number;
  symbol?: string;
  at?: number;
  riskUsd?: number;   // ← חדש: |entryPrice - stopLoss| × quantity, מצולם בכניסה
}
```

אופציונלי בכוונה: ב-Firestore יש היסטוריית עסקאות קיימת בלי השדה, והמערכת חייבת להמשיך לעבוד עליה.

**1b — צילום הסיכון בכניסה**

⚠️ **נקודה קריטית:** יש לצלם את הסיכון **ברגע הכניסה**, לא בסגירה. `pos.stopLoss` זז בגלל trailing stop ו-reanchor, כך שחישוב בסגירה יחזיר מספר שגוי.

- `SimPosition` (`simExecution.ts:72-109`) — הוספת `initialRiskUsd: number`, מאוכלס בפתיחת פוזיציה
- אכלוס `riskUsd` בבניית רשומת הסגירה בשלושת המסלולים: `simExecution.ts` (מלא + `partial_tp1`), `legacySimExecution.ts`, `proSimExecution.ts`
- ב-`partial_tp1` — חצי מהסיכון, במקביל ל-`halfPnl` הקיים
- `backtestRunner.ts:515,521` — `state.closedTrades.push({ pnl, at })` צריך גם הוא `riskUsd`, אחרת הבקטסט ימדוד מנוע שונה מהייצור

**1c — שימוש ב-R-multiples עם נסיגה מסודרת**

בשני המנועים (`tradeEngine.ts:1328-1333`, `proAlgEngine.ts:617-621`), זהה:

```
אם לכל העסקאות בחלון יש riskUsd  →  R = ממוצע(pnl/riskUsd | זוכות) / |ממוצע(pnl/riskUsd | מפסידות)|
אחרת                              →  הנוסחה הדולרית הקיימת (תאימות לאחור)
```

**1d — הקשחת פרמטרים**

| פרמטר | היום | מוצע | נימוק |
|---|---|---|---|
| סף מינימלי | 30 עסקאות | **100** | ±9.1pp → ±5.0pp שגיאת תקן |
| מקדם קלי | 0.5 (חצי) | **0.25 (רבע)** | סטנדרט מקובל כשה-edge מוערך ולא ידוע |
| תקרה | 0.10 | 0.10 (ללא שינוי) | — |
| ברירת מחדל טרום-סף | 0.06 | 0.06 (ללא שינוי) | — |

### אימות שלב 1

- טסט יחידה חדש: אותו רצף עסקאות בגדלים שונים חייב להניב R **זהה** ב-R-multiples ושונה בשיטה הדולרית — זה הטסט שנכשל על הבאג ועל שום דבר אחר
- טסט נסיגה: מערך עסקאות ללא `riskUsd` מחזיר בדיוק את התוצאה הנוכחית
- בקטסט A/B (שלב 4)

**Rollback:** תג `checkpoint-kelly-dollar-r` לפני השינוי.

### ✅ בוצע — 2026-09-03

בוצע כמתוכנן, **למעט סעיף 1d שנדחה על סמך מדידה** ובתוספת באג שהתגלה תוך כדי.

| תת-שלב | סטטוס |
|---|---|
| 1a — `riskUsd` ב-3 טיפוסי הרשומה | ✅ |
| 1b — צילום בכניסה + העברה ב-7 נקודות מיפוי | ✅ |
| 1c — `kellyPayoffRatio()` עם נסיגה מסודרת | ✅ |
| 1d — סף 30→100 | ❌ **נדחה — מדידה הראתה נזק חמור** |
| 1d — חצי→רבע קלי | ✅ |

**❌ למה סף 100 נדחה.** הטיעון הסטטיסטי נכון, אבל המדידה הראתה: Pro ירד מ-**+$17.66 ל-−$103.21** וה-drawdown עלה מ-1.66% ל-7.22%.

הסיבה היא **הנסיגה, לא הסף**. מתחת לסף המנועים מתמחרים ב-**6% שטוח בלי שום משוב על ה-edge**, ולכן העלאת הסף פשוט קונה עוד עסקאות שנלקחות בעיוורון. קלי רועש עדיין מגן, כי הוא מתאפס על ספר מפסיד — וההגנה מגיעה מ**הסימן** של ה-edge, ש-30 עסקאות כן מספיקות לו, לא מהגודל שלו. **התיקון האמיתי הוא לעשות את המסלול התת-סִפִּי מודע-edge; עד אז הסף נשאר 30.** טסט מקבע את הקבוע כדי שהעלאה עתידית תהיה מעשה מודע.

**🐛 באג שהתגלה: פוזיציות בגודל אפס.** ה-R-multiples חשפו עסקאות עם `riskUsd` בדיוק 0. קלי מאפס את `betFraction` כשה-edge שלילי, ומעקף ה-confidence ≥ 72 העביר את זה כפוזיציה עם `quantity` 0 — שנסגרה ב-pnl בדיוק 0. **לא רווח ולא הפסד, אבל כן נספרה ב-`closedTrades.length`** — כלומר ניפחה את המכנה של שיעור ההצלחה בקלי ודיכאה את ההערכה שיצרה אותה. זה היה **רוב מספר העסקאות של Pro: 159 → 38.**

#### תוצאות A/B מול קווי הבסיס

| מדד | Pro לפני | Pro אחרי | Legacy לפני | Legacy אחרי |
|---|---|---|---|---|
| עסקאות | 59 | 38 | 33 | 31 |
| רווח נקי | −$3.36 | **+$38.42** | −$19.92 | −$19.92 |
| Profit factor | 0.984 | **1.239** | 0.885 | 0.885 |
| תוחלת | −$0.06 | **+$1.01** | −$0.60 | −$0.64 |
| Max drawdown | 3.01% | **1.37%** | 1.16% | 1.16% |

ה-PnL של Legacy זהה **לסנט** — שתי העסקאות שהוסרו היו פוזיציות רפאים בגודל אפס.

---

## שלב 2 — נרמול SL/TP לפי ATR ב-Legacy ו-Pro

**סיכון:** בינוני-גבוה (משנה כל עסקה) | **מאמץ:** ~4 שעות | **תלות:** שלב 1

### הבעיה

```ts
// tradeEngine.ts:1275-1280 — וזהה ב-proAlgEngine.calculateProRisk
const fixedSlPercent = 1.8;
const fixedTpPercent = 3.0;
```

סטופ קבוע של 1.8% על BTC ועל UNI הוא טעות מדידה בסיסית — התנודתיות היומית ביניהם נבדלת פי 2-3. על נכס תנודתי הסטופ נפגע מרעש; על נכס רגוע הוא רחוק מדי והיחס סיכון/סיכוי נשחק.

### למה זה זול

1. **התשתית כבר קיימת ומנותקת:** `slMin`/`slMax` מחושבים ב-`tradeEngine.ts:1272-1273` מ-`MIN_STOP_PERCENT`/`MAX_STOP_PERCENT` ו**אינם בשימוש בשום מקום בקובץ**. הפרמטר `slConfig` לסריקת בקטסט כבר בחתימה.
2. **יש מימוש ייחוס בתוך הפרויקט:** מנוע ה-Intraday כבר עושה זאת נכון — `minStopAtrMult: 0.8` ([intradayParams.ts:225](packages/engine/src/services/intradayParams.ts#L225)), עם `intradayRisk.ts` כמודל.

### הביצוע

- החלפת `fixedSlPercent` ב-`atr × slAtrMult`, מוצמד (clamped) ל-`[slMin, slMax]` — כלומר החייאת `slMin`/`slMax`
- שמירת יחס `riskRewardRatio = 1.67` כך ש-TP נגזר מהסטופ ולא נקבע בנפרד
- TP2 נשאר `tpDistance × 1.5`

⚠️ **צימוד שיש לשים לב אליו:** `riskRewardRatio` משמש כנסיגה בחישוב הקלי (`historicalR = ... : riskRewardRatio`). שינוי הגאומטריה משנה גם את הקלי — לכן שלב 2 **חייב** לבוא אחרי שלב 1, לא לפניו, אחרת שני שינויים מתערבבים בבקטסט אחד ואי אפשר לייחס תוצאה.

### אימות שלב 2

- טסט: שני סימבולים עם ATR שנבדל פי 3 מקבלים מרחקי סטופ שונים, שניהם בתוך ה-clamp
- בקטסט A/B, בדגש על **התפלגות ה-R-multiples** ולא רק על PnL

**Rollback:** תג `checkpoint-fixed-sl-tp`.

### ✅ בוצע — 2026-09-03

הסטופ הוא כעת `ATR × SL_ATR_MULTIPLIER` מוצמד ל-`[MIN_STOP_PERCENT, MAX_STOP_PERCENT]`, וה-TP **נגזר** ממנו ביחס `SL_TP_REWARD_RISK` (1.67, ללא שינוי) — כך שיחס הסיכון/סיכוי קבוע בכל משטר תנודתיות. טסט מאמת את האינווריאנט ישירות.

#### סריקת מכפיל ה-ATR (Profit factor: Pro / Legacy)

| מכפיל | Pro | Legacy |
|---|---|---|
| 1.0 | 1.191 | 0.796 |
| **1.2** | **1.315** | **0.889** |
| 1.5 | 1.320 | 0.873 |
| 2.0 | 1.131 | 0.849 |
| 2.5 | 1.058 | 0.843 |

⚠️ **1.2 ו-1.5 שקולים סטטיסטית** על ~40 עסקאות. הממצא היציב הוא **הצורה**: האזור 1.2-1.5 מנצח כל מה שמחוצה לו, ו-≥2.0 גרוע בבירור בשני המנועים. 1.2 נבחר בזכות ה-drawdown הטוב יותר — **לא כי הוא אופטימום מאותר**. הטסטים גוזרים ציפיות מהקבועים ולא מקודדים אריתמטיקה, כדי שכיול מחדש יכוון אותם במקום לשבור אותם.

**Pro:** PF 1.239 → **1.315**, drawdown 1.37% → **0.90%**.
**Legacy:** PF 0.885 → 0.889, drawdown 1.16% → 1.10%.

> **Legacy עדיין מפסיד כסף בכל וריאנט שנוסה.** גם הסייזינג וגם גאומטריית הסטופ תוקנו עליו והוא נשאר לא-רווחי בחלון הזה. זה מצביע על **שכבת האותות שלו, לא על שכבת הסיכון**.

---

## שלב 3 — Funding Rate כגורם אורתוגונלי

**סיכון:** נמוך (וטו/סייזינג בלבד) | **מאמץ:** ~8-10 שעות | **תלות:** שלב 4 עובד

### הנימוק

שבעת האינדיקטורים הנוכחיים — MACD 20, EMA 18, RSI 12, BB 12, Volume 18, Supertrend 12, Stoch 8 — קולינאריים מאוד. MACD, EMA-cross ו-Supertrend הם שלוש נגזרות של אותו ממוצע נע; RSI ו-Stochastic הם שני אוסילטורים של אותו מומנטום. ה-breadth האפקטיבי הוא כ-2, לא 7.

Funding rate הוא **המדד היחיד בקריפטו עם שווי הוגן אכיף בארביטראז'**, והוא מודד **מיצוב** ולא מגמה — כלומר אורתוגונלי אמיתית לכל השבעה. אומת: אין בקוד היום שום נתון funding/basis/openInterest.

### הביצוע

**3a — שכבת נתונים**
- `marketDataService.ts` — `fetchFundingRates()` מול Binance `GET /fapi/v1/premiumIndex` (ציבורי, ללא מפתח, ללא עלות)
- מיפוי סימבולים דרך `symbolUniverse.ts` הקיים — הוא כבר מטפל בסימבולים שאינם נסחרים (הלקח מבאג "CORS" שהיה 400 Invalid symbol)
- כשל בשליפה = **abstain**, לא חסימה. הבוט חייב להמשיך לעבוד בלי הנתון

**3b — התמדה**
מטמון דרך `kvStore.ts` (כבר עמיד דרך Firestore). Funding מתעדכן כל 8 שעות — אין צורך בקצב גבוה.

**3c — שער החלטה**
`PipelineStage` חדש ב-`proAdapter.ts`, **אחרי** חישוב הסיכון:

- **לא טריגר כניסה.** מודיפייר סייזינג + וטו בלבד. זה מה ששומר על הזרימה הקיימת ומוריד את סיכון הרגרסיה
- funding חיובי קיצוני ומתמשך = לונגים עמוסים → הקטנת `betFraction` או וטו על לונג
- **הסף ב-z-score או ב-bps שנתיים — לא באחוזי מחיר.** זו בדיוק הנקודה שבה ההצעה המקורית של 8% נשברה

**3d — חובה: מטא-דאטה של השער**
כל החזרת `blocked: true` חייבת לכלול `gate` ו-`blockReason` אמיתיים, אחרת חוזרים לבאג `[UNKNOWN] / Blocked`. יש להוסיף `FUNDING` ל-`KNOWN_GATES` ב-[decisionBlockMetadata.test.ts:113](src/__tests__/decisionBlockMetadata.test.ts#L113) ולהוסיף טסט ייעודי.

### אימות שלב 3

- טסט: נתוני funding חסרים → ההחלטה זהה לחלוטין למצב ללא השכבה
- טסט: funding קיצוני → `gate === 'FUNDING'` עם נימוק לא ריק
- מדידת המתאם בפועל בין ציון ה-funding ל-`SignalScore` הקיים. **אם |ρ| > 0.3 — הגורם אינו אורתוגונלי והשלב לא מצדיק את עצמו**

### ✅ בוצע — 2026-09-03

| תת-שלב | סטטוס |
|---|---|
| 3a — `fetchFundingRates()` מול Binance `/fapi/v1/premiumIndex` | ✅ קריאה אחת לכל היקום, מטמון 30 דק' |
| 3b — התמדה/מטמון | ✅ מטמון בזיכרון; כשל אינו נשמר במטמון |
| 3c — `FundingGateStage` ב-`proAdapter` | ✅ אחרי ניתוב, לפני סיכון |
| 3d — מטא-דאטה של השער + טסטים | ✅ `FUNDING` נוסף ל-`KNOWN_GATES`, 12 טסטים חדשים |

**✅ מבחן העצירה עבר.** `scripts/fundingOrthogonality.ts` מודד את המתאם בפועל בין ה-funding לבין מה ש-Layer 1 של Pro היה אומר, בכל הסדרה ההיסטורית:

| סימבול | n | ρ |
|---|---|---|
| BTCUSDT | 535 | −0.005 |
| ETHUSDT | 535 | 0.144 |
| SOLUSDT | 535 | 0.136 |
| AVAXUSDT | 535 | 0.232 |
| DOGEUSDT | 535 | 0.259 |
| LINKUSDT | 535 | 0.228 |
| **מאוחד** | **3,210** | **0.169** |

ρ=0.169 — הרבה מתחת לסף 0.3, וכל סימבול בנפרד גם הוא מתחת. **הגורם אכן אורתוגונלי** ומוסיף מידע שאינדיקטורי המחיר אינם נושאים.

**🔧 כיול מחדש שהמדידה חייבה.** הספים הראשוניים (50%/100% שנתי) **מעולם לא נורו** — 0 מתוך 3,156 אותות. התפלגות ה-funding שהצד המאותת משלם בפועל:

```
p50 3.1   p75 8.8   p90 10.9   p95 10.9   p99 17.5   max 45.8   (%/שנה)
```

הספים ישבו מעל אפילו המקסימום. כוילו מחדש ל-**25% (trim) / 50% (veto)** — כ-2.3x ו-4.5x מקו הבסיס של Binance (0.01% ל-8 שעות = ~10.95%/שנה). שיעור הירי כעת: **0.4% trim, 0% veto**.

זו ההתנהגות הנכונה, לא כשל: **צפיפות היא תופעת זנב.** שער שנורה באופן שגרתי בשוק רגוע היה מוגדר לא נכון. ה-veto הוא ביטוח מפני תנאי מאניה, שבהם funding על אלטים היסטורית רץ 100-300%/שנה מיד לפני סקוויז.

#### מגבלת מדידה — נאמרת במפורש

**השער אינו מכוסה על ידי רתמת ה-A/B.** ה-backtest עוקף את ה-pipeline של ה-adapter וקורא ל-`calculateProRisk` ישירות, ולכן `marketData.funding` לעולם אינו מאוכלס שם. אומת: ריצת `step3-pro` החזירה תוצאה **זהה** ל-`step2-pro` ($44.10, PF 1.315, DD 0.90%) — כלומר השער נמנע בנקיון כשאין נתונים, בדיוק כמתוכנן.

לכן השער נוסף על **בסיס תכן ולא על בסיס תוצאה**, ומה שמגביל את סיכונו הוא המבנה: הוא יכול רק לפסול או לצמצם, לעולם לא ליזום; הוא נמנע על נתון חסר או ישן; והוא כוסה ב-12 טסטים שמקבעים בדיוק את התכונות האלה.

---

## שלב 4 — רתמת מדידה (תנאי מקדים לשלבים 1-3)

**סיכון:** אפס | **מאמץ:** ~3 שעות | **תלות:** שלב 0

בלי זה, שלבים 1-3 הם ניחוש. יש לבנות זאת **לפני** שמשנים לוגיקה.

### מה נדרש

- סקריפט A/B שמריץ את `backtestRunner` על סט סימבולים וטווח תאריכים **קבועים** (seed קבוע), לפני ואחרי כל שלב
- קלט זהה לחלוטין בין הריצות — אחרת ההשוואה חסרת ערך

### מדדים — לא PnL בלבד

| מדד | למה |
|---|---|
| Sharpe / Sortino | PnL בלי התאמה לסיכון חסר משמעות |
| Max Drawdown | המדד שקובע אם המערכת שרידה |
| מספר עסקאות | שינוי גדול = שינוי אסטרטגיה, לא שיפור |
| **התפלגות R-multiples** | המדד היחיד שמראה אם ה-edge אמיתי או תוצר סייזינג |
| Profit Factor | — |

### מגבלות נאמנות שנותרו (יש לתעד בכל דוח)

- ✅ SL/TP תוך-נרי — **קיים** (`intrabarExit`, `backtestRunner.ts:291-315`), כולל טיפול בגאפים והעדפת SP על TP באותו נר
- ❌ אין עלויות funding בהחזקת פוזיציות Futures — מטה תוצאות כלפי מעלה בהחזקות ארוכות
- ❌ חישוב אינדיקטורים מלא בכל נר (O(n²)) — מגביל את אורך הריצה, לא את נכונותה
- ❌ **יציאות הזמן תמיד "פגות" בבקטסט** — ראו הממצא בסעיף הביצוע למטה

### ✅ בוצע — 2026-09-03

**נבנה:** [`scripts/abBacktest.ts`](scripts/abBacktest.ts) — שלוש פקודות:

```bash
npx tsx scripts/abBacktest.ts snapshot --from 2025-01-01 --to 2025-07-01
npx tsx scripts/abBacktest.ts run --label baseline-pro --engine pro
npx tsx scripts/abBacktest.ts compare baseline-pro <candidate>
```

**עקרונות התכן:**
- `snapshot` מוריד נרות לטווח תאריכים **מוחלט** (לא יחסי ל-`Date.now()`) ושומר לדיסק. כל ריצה מנגנת מחדש את אותו קובץ
- `run` משתמש ב-**`SlConfig` קבוע יחיד**, לא בגריד — כדי שההבדל היחיד בין ריצות יהיה קוד המנוע
- `compare` **מסרב להשוות** ריצות עם חלון תאריכים שונה או מנוע שונה, ומתריע כשמספר העסקאות זז מעל 10% (סימן שהשינוי החליף אילו עסקאות נלקחו, לא רק את גודלן)
- פלט ל-`backtest-ab/` (נוסף ל-`.gitignore`)

**שינוי ב-`backtestRunner.ts` — אדיטיבי בלבד, אפס שינוי התנהגותי:** ייצוא `runBacktest` / `runPortfolioBacktest` / `BacktestResult`, והוספת שדה `closedTrades` למבנה המוחזר (הרתמה זקוקה לסדרת ה-PnL הגולמית; המצרפים לא מספיקים לפיזור).

#### קווי בסיס שנרשמו (6 סימבולים, 2025-01-01 → 2025-07-01, 26,070 נרות)

| מדד | Pro | Legacy |
|---|---|---|
| עסקאות | 59 | 33 |
| Win rate | 37.3% | 39.4% |
| רווח נקי | −$3.36 | −$19.92 |
| Profit factor | 0.984 | 0.885 |
| תוחלת לעסקה | −$0.06 | −$0.60 |
| Max drawdown | 3.01% | 1.16% |
| Sharpe/עסקה | −0.005 | −0.051 |
| R-multiples | לא זמין | לא זמין |

**דטרמיניזם אומת:** שתי ריצות של אותה קונפיגורציה החזירו תוצאה **זהה ביט-לביט** בכל המדדים.

> ⚠️ **שני קווי הבסיס מפסידים כסף על החלון הזה.** זו נקודת המוצא למדידה, לא ממצא על טיב האסטרטגיה — חלון של חצי שנה אחת בשישה סימבולים אינו ראיה מספקת לכאן או לכאן. משמעותו המעשית: שלבים 1-2 צריכים להראות שיפור ב**תוחלת** ו-**Sharpe/עסקה**, לא רק ב-PnL.

#### ממצא חדש שהרתמה חשפה

`evaluateExit` מחשב את משך ההחזקה מול שעון הקיר:

```ts
// tradeEngine.ts:1553
const heldMs = Date.now() - (pos.openTimestamp || Date.now());
```

בבקטסט `openTimestamp` הוא חותמת הנר ההיסטורי, ולכן `heldMs` תמיד עצום — **כל פוזיציה נחשבת מיידית כמוחזקת מעל 24 ו-48 שעות**. התוצאה: יציאת הזמן של Futures (צמצום 50% אחרי 24 שעות ללא TP1) ויציאת ה-Spot של 48 שעות נבדקות על תנאי המחיר בלבד, כבר בנר הראשון.

- **על הדטרמיניזם:** אין השפעה. התנאי תמיד אמיתי, ולכן יציב בין ריצות — ה-A/B תקף
- **על הנאמנות:** יש. הבקטסט מפעיל יציאות זמן שבחי לא היו נורות עדיין
- **לא תוקן כאן** — זו לוגיקת בוט. מועמד לשלב נפרד; יש להעביר שעון מוזרק (injected clock) במקום `Date.now()`

---

## סדר ביצוע ותלויות

```
שלב 0 (ניקוי + פריסה)
   │
   ├──→ שלב 4 (רתמת מדידה)  ←─── תנאי מקדים לכל השאר
   │        │
   │        ├──→ שלב 1 (קלי)         ← התשואה הגבוהה ביותר
   │        │       │
   │        │       └──→ שלב 2 (ATR)  ← חייב לבוא אחרי 1 (צימוד riskRewardRatio)
   │        │
   │        └──→ שלב 3 (funding)      ← רק אם 1 ו-2 יציבים
```

| שלב | מאמץ | סיכון | תשואה צפויה |
|---|---|---|---|
| 0 — ניקוי + פריסה | 0.5 ש' | אפס | מסלק חוב וסתירות; מפעיל את `c8d83ba` |
| 4 — רתמת מדידה | 3 ש' | אפס | מאפשר לדעת אם משהו עבד |
| 1 — קלי | 4-6 ש' | בינוני | **הגבוהה ביותר** — מסלק לולאת משוב אמיתית |
| 2 — ATR | 4 ש' | בינוני-גבוה | גבוהה — מחבר תשתית קיימת ומנותקת |
| 3 — funding | 8-10 ש' | נמוך | בינונית — מותנית במבחן המתאם |

**מומלץ להתחיל ב-0 ו-4 בלבד** — שניהם חסרי סיכון, ו-4 הוא מה שיקבע אם 1 ו-2 בכלל משפרים משהו.

---

## סיכום החלטות

| החלטה | נימוק |
|---|---|
| לא מיישמים fair value / 8% | סף לא תואם לגאומטריית SL 1.8% / TP 3.0%; חופף לאינדיקטורים קיימים |
| הקלי לפני ה-ATR | `riskRewardRatio` מצמיד ביניהם — הפוך יערבב שני שינויים בבקטסט אחד |
| funding כווטו, לא כטריגר | שומר על הזרימה הקיימת; מוריד סיכון רגרסיה דרמטית |
| `riskUsd` אופציונלי | היסטוריה קיימת ב-Firestore חייבת להמשיך לעבוד |
| רתמת מדידה לפני כל שינוי | בלעדיה אין דרך לדעת אם שלב הצליח |
